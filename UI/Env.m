classdef Env
	%ENV Setup the environment.
	%   Setup the environment

	properties
        currentData
        referencePacket
        referenceChecksum
    end

	methods
        function obj = updateData(obj)
        end
    end
end